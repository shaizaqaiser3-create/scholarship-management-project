# Scholarship Management Project

This is a PHP-based system to manage scholarship applications.

## How to Run
1. Upload these files to your local server (like XAMPP).
2. Import the database.
3. Open `index.php` in your browser.
